<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1C1C1E"
    tools:context=".MainActivity"> <!-- Changed context to .Home -->

    <!-- Fragment Container: This is where fragments will be displayed -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom Navigation -->
    <LinearLayout
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:background="#2C2C2E"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- NOTE: Replace src with your actual drawable icons if different -->
        <!-- ID Naming Convention: nav_<conceptual_name> -->
        <ImageView
            android:id="@+id/nav_home"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:src="@drawable/house_regular_full"
            app:tint="#FFD600" /> <!-- Assuming Home is initially selected -->
        <ImageView
            android:id="@+id/nav_transactions"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:src="@drawable/readme_brands_solid_full"
            android:contentDescription="Transactions"
            app:tint="#BDBDBD"/>
        <ImageView
            android:id="@+id/fab_add_transaction"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:src="@drawable/circle_plus_regular_full"
            android:contentDescription="Add Transaction"
            app:tint="#BDBDBD"/> <!-- This is treated as a FAB/action button -->
        <ImageView
            android:id="@+id/nav_budgets"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:src="@drawable/piggy_bank_regular_full"
            android:contentDescription="Budgets"
            app:tint="#BDBDBD"/>
        <ImageView
            android:id="@+id/nav_profile"
            android:layout_width="0dp"
            android:layout_height="32dp"
            android:layout_weight="1"
            android:src="@drawable/outline_account_circle_24"
            android:contentDescription="Profile"
            app:tint="#BDBDBD"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>